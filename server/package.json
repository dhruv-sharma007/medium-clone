{
  "name": "server",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "format": "prettier --write './src/**/*.{js,ts,jsx,tsx}'"
  },
  "dependencies": {
    "@medium-clone/common": "^1.0.4",
    "@prisma/client": "^6.9.0",
    "@prisma/extension-accelerate": "^2.0.1",
    "hono": "^4.7.11",
    "prisma": "^6.9.0"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}